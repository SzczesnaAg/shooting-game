{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^22.13.2",
    "phaser": "^3.60.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.14",
    "gh-pages": "^6.0.0"
  },
  "type": "module"
}
